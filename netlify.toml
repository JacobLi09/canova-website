# Netlify configuration for Canova website
# Static site deployment with redirects for SPA support

[build]
  publish = "."
  command = "echo Done"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
